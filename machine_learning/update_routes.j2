{% set n = range(101, 110) | random -%}
replace:
routing-options {
    static {
        route 0.0.0.0/0 next-hop 100.123.0.1;
        {% for item in range(1, n) -%}
        route 10.0.0.{{item}}/32 discard;
        {% endfor %}
    }
}
